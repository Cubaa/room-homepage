import React, { FC } from "react";
import { MobileMenuBackdropContainer } from "./mobile-menu-styled";

export const MobileMenuBackdrop: FC = () => {
  return <MobileMenuBackdropContainer></MobileMenuBackdropContainer>;
};
